import { InvoiceNumberDirective } from './invoice-number.directive';

describe('InvoiceNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new InvoiceNumberDirective();
    expect(directive).toBeTruthy();
  });
});
